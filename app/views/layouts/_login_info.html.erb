<li class="nav-item dropdown">
  <a href="#account_menu" class="nav-link dropdown-toggle" data-toggle="dropdown" data-caret="false">
    <img src="<%= image_path(current_user.avatar_url) %>" class="rounded-circle" width="32" alt="Frontted">
    <span class="ml-1 d-flex-inline">
      <span class="text-light"><%= current_user.username %></span>
    </span>
  </a>
  <div id="account_menu" class="dropdown-menu dropdown-menu-right">
    <%= link_to "Invite Member", new_invitation_path, class: 'dropdown-item' %>
    <%= link_to "Billing", subscriptions_path, class: 'dropdown-item' %>
    <div class="dropdown-divider"></div>
    <%= link_to('Logout', destroy_session_path, method: :delete, class: 'dropdown-item') %> 
  </div>
</li>